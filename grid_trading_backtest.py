import pandas as pd \nimport numpy as np \nimport matplotlib.pyplot as plt \n\ndef grid_trading_backtest(prices, grid_size, investment): \n    grid_levels = np.arange(prices.min(), prices.max(), grid_size) \n    holdings = 0 \n    cash = investment \n    grid_orders = [] \n\n    for price in prices: \n        if price <= grid_levels[0]: \n            # Buy at the lowest grid level \n            amount = cash / price \n            holdings += amount \n            cash = 0 \n            grid_orders.append(('buy', price, amount)) \n        elif price >= grid_levels[-1]: \n            # Sell at the highest grid level \n            cash += holdings * price \n            grid_orders.append(('sell', price, holdings)) \n            holdings = 0 \n\n    total_value = cash + holdings * prices[-1] \n    return total_value, grid_orders \n\n# Example usage \n# prices = pd.Series(...)  # Replace with actual price data \n# total_value, orders = grid_trading_backtest(prices, grid_size=0.1, investment=1000) \n# print("Total Value:", total_value) \n# print("Orders:", orders)